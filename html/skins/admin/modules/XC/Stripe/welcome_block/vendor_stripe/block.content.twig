{##
 # Block content : items
 #
 # @ListChild (list="welcome-block.vendor-stripe.content", weight="10")
#}
<div class="content">
    <div class="stripe-logo"></div>
    <div class="info">
        <h2>{{ t('To accept online payments, you need a Stripe account')|raw }}</h2>
        <div>{{ t('You can connect existing Stripe account or create a new one on the [Financial Info page].', {'financialTabURL': this.getFinancialTabURL()})|raw }}</div>
    </div>
</div>